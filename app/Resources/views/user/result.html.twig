{% extends 'base.html.twig' %}

{% block body %}
    {% if empty %}
        <h1>Geen vragen gevonden</h1>
    {% else %}
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    {% endif %}
{% endblock %}
